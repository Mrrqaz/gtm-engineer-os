# Goals (real)

- Land a founder-adjacent operating role — GTM Engineer, Growth Engineer, or RevOps Engineer at an AI-native or fast-growing B2B SaaS company — where the job is building the systems that generate pipeline, not running campaigns by hand.
- Show, not tell: this repo is proof I already think and build this way, using Autoage (my own lead-gen company) as the real-world testbed — enrichment waterfalls, signal-based outbound, CRM/data hygiene, and reply/campaign automation are things I've built and run, not just read about.
- Keep Autoage running as a side asset regardless of the outcome of this search (see the dual-track decision — not detailed here since it's Autoage-internal, not part of this public repo).
- Prioritize roles where I'd be trusted to own systems end to end (build → ship → monitor → iterate), not just execute a playbook someone else designed.
